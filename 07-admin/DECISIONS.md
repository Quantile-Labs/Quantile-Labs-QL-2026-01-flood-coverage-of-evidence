# Decisions log — QL-2026-01

**Append only.** Wrong entries are superseded by a new row, never edited.

Log whenever you: exclude a record, change a threshold, choose between two defensible
definitions, resolve a borderline case, abandon a planned analysis, find a bug, or rule
something out of scope.

Test: *would a stranger reading the results wonder why you did that?* Then log it — with the
reason, written at the moment of deciding.

| Date (UTC) | Decision | Rationale |
|---|---|---|
| 2026-08-03 | **Day-1 join check PASSES. QL-2026-01 proceeds.** Per-gauge metrics join to coordinates and a country at 5,678/5,678 globally (100%) and 741/741 for Africa (100%). | Metric files are named `GRDC_<id>.csv`. `metadata/basin_county.csv` maps `gauge_id` → country for all 5,678; `metadata/grdc_stations_20220320.csv` supplies lat/long for all 5,678, none null, none at (0,0). No fallback needed — GRDC registration is not required and is not pursued. Run logged in RUNLOG.md. |
| 2026-08-03 | Downloaded `metrics.tgz`, `metadata.tgz` and `gauge_groups_for_paper.tgz` only. `model_data.tgz` (14.5 GB) and `trained-models.tgz` (3.7 GB) deliberately not acquired. | Not needed at any point in this design — the study reanalyses released metrics and never re-runs the model. All three md5s match the values published by the Zenodo API. |
| 2026-08-03 | Added `gauge_groups_for_paper.tgz` (91.6 kB), which the day-1 instruction did not call for. | It carries `continent_splits/africa.txt`, `country_splits/` and `grdc_filtered.txt` — the developer's own definition of which gauges are African and which are in the evaluation set. Using their grouping rather than inferring one from coordinates removes a whole class of our own judgement calls from the denominator. |
| 2026-08-03 | **Open, to be resolved in the protocol: 5,678 gauges have a per-gauge metric file, but the Nature paper reports an evaluation set of 4,089.** Not resolved today, on purpose. | The gap is most likely a reliability or record-length filter applied after the files were written, so a file existing may not mean a usable metric was published. Resolving it means inspecting metric contents, and "has a published metric" is this study's primary outcome — its definition must be frozen before it is computed, not chosen after seeing which definition gives the more interesting number. The protocol defines the rule, including NaN handling, and only then is it computed. |
| 2026-08-03 | Day-1 check written as a script under `03-harness/` and blinded by construction: it reads gauge identifiers, coordinates and countries, and never opens a metric file. Gauge lists come from `tar tzf` archive listings, so no metric value is decompressed. | Feasibility work touches the same tree the analysis will. Doing it blind on day 1 costs nothing and means the strata are built before the outcome exists, per the standing rule. The one exception is recorded in the next row. |
| 2026-08-03 | Read the *schema* of a single metric file — column and row labels only, no values: rows are return periods 1.01/2/5/10/20/50 (three rows each), columns are lead times 0–7. | The protocol cannot specify a primary metric without knowing what shape the metric table is. Labels are structure, not outcome. Recorded here so a reader can see exactly what was seen and when. |
| 2026-08-03 | Noted for the protocol, not acted on: `metadata/hybas_gauges_info_lev12.csv` holds 41,905 gauges (6,772 in Africa) against 5,678 evaluated. **This is not yet established to be the Flood Hub deployment surface** and must not be used as the denominator until it is. | It is the gauge inventory shipped with the paper, which is a different thing from the ~5,000 verified and ~240,000 lower-confidence points the product displays. Neither of those product figures appears in this release at all. Getting the denominator wrong is the single fastest way to make this Note dishonest. |
| 2026-08-05 | **OSM stratum complete: 229,744 / 230,028 basins (99.9%).** The 284 without a value are 146 offshore, 79 behind a junk code, and 59 in Spain, Yemen, Portugal and France — all outside the study region. **Within Africa, coverage is complete.** | Stratum 3 is fully populated, so no basin enters the sparse tercile for want of processing. |
| 2026-08-05 | **The tercile deviation applies continent-wide, not just in sparse countries: 39.3% of African basins have ZERO OSM features.** Chad's 52.2% was not an outlier. Final cut: T1_sparse 90,373 (all zero-feature), T2 69,686, T3_dense 69,685. | Confirms the deviation declared on country 1 was necessary rather than a local quirk, and that it was fixed before the continental number was known. Had `qcut` been left alone, roughly 90,000 basins would have been silently redistributed across two bins presented as three. |
| 2026-08-05 | **Feature mix confirmed continent-wide: 86.4% buildings (128,999,163) to 13.6% roads (20,325,029).** Stratum 3 is settlement mapping density, as PROTOCOL §8 assumes. | Checked at continental scale, not extrapolated from Chad's 95.1/4.9. The proportion moved but the conclusion holds. |
| 2026-08-05 | **Lesotho failed the full OSM run with a transient drop while its file was present and served fine on retry.** The OSM harness had no retry logic; the population harness did. Added, and Lesotho recovered — 230 basins. | The same class of failure that nearly cost DR Congo's 17,900 basins, and it recurred because the fix was applied to one script and not its sibling. A country lost to a flaky connection is not a random omission. |
| 2026-08-05 | **WorldPop layer complete: 228,344 / 230,028 basins (99.3%).** DR Congo recovered on retry — 17,735 of 17,900 basins. Independent sanity check: the zonal sums total **1,322,374,091** people against a UN 2020 Africa estimate of ~1.34 bn, agreeing to about 1.3%. | The agreement is a check on the zonal-sum implementation — projection handling, nodata exclusion, double counting — not a result. Recorded because a silent 2x or 0.5x error in this step would propagate into every population-weighted figure and would be invisible in the outputs. |
| 2026-08-05 | **The 1,684 basins with no population value are accounted for rather than averaged over: 146 have no country (offshore), 79 sit behind a junk geoBoundaries code, and of the 1,538 remaining, 976 fall in Lake Victoria (369), Tanganyika (280), Malawi (211), Turkana (68), Albert/Kyoga (33), Nasser (10) and Chad (5).** They are open water, where WorldPop carries nodata. **562 remain unexplained** and are reported as such. | Left as NA, not zero: the raster does not say "nobody lives here", it says nothing. Immaterial to population-weighted figures, since open water holds no people, but material to basin counts, so both are reported. The 562 are named rather than folded into the lake explanation — the tidy story covers 63% of the cases and claiming it covers all of them would be the kind of small overreach this study cannot afford. |
| 2026-08-05 | Junk non-ISO codes from geoBoundaries (e.g. `"111"`) are filtered from the country list. Basins behind them are still counted as unassigned and reported. | A permanent "NO RASTER" line in the completion report would read as a real coverage gap. Suppressing the noise makes a genuine failure visible when one occurs. |
| 2026-08-05 | **`_lib/manifest.py` gains a `transient` column**, for inputs that are hashed and then deliberately deleted. Absent transient files pass `--verify`; present ones are still hash-checked; absent non-transient files still fail. Existing manifests migrated, 15 streamed files marked retroactively by their `_scratch_` paths, with no pre-existing field altered and every sha256 confirmed identical. | Streaming made `--verify` fail on every run, which trains the operator to ignore a check whose whole value is failing loudly. A provenance tool nobody reads is worse than none. Shared-library change, so the migration was made non-destructive and verified rather than trusted. |
| 2026-08-05 | **Protocol v1.6.** sha256 `9061f7897a940f58ede84a16da9374d67572069f85fa1ada03c539d138823bd7`, tag `QL-2026-01-protocol-v1.6`. | OSM stratum deviation declared, feature mix recorded, zero-vs-unprocessed fixed. |
| 2026-08-05 | **DEVIATION declared before it could be tuned: equal-frequency terciles of OSM density do not exist in Chad — 5,045 of 9,667 basins (52.2%) are at exactly zero.** `qcut` would have collapsed to two bins silently. Replacement, fixed now on the first country: zero share > 1/3 ⇒ `T1_sparse` = zero, `T2`/`T3` = median split of the non-zero remainder; otherwise equal-frequency as written. Zero share reported per country and in the limits box. | In the sparsest places the honest distribution is a point mass at zero, not a smooth one. Choosing the cut rule after seeing which gave better contrast would be precisely the post-hoc tuning pre-registration exists to prevent, so it is fixed on country 1 of 55. |
| 2026-08-05 | **Bug found and fixed: zero features and unprocessed were being conflated.** The first version stored a basin with no OSM features as NA, identical to a basin in a country never downloaded. That silently dropped Chad's least-mapped 52% out of stratum 3 and would have biased the tercile cut towards better-mapped ground. Now: processed extract + no features ⇒ **0**; country never downloaded ⇒ **NA**. | Self-refuting for a study whose subject is the difference between "nothing is there" and "nothing was measured". The same principle was already applied to raster nodata; it should have been applied here from the start. |
| 2026-08-05 | **Checked rather than assumed — the OSM feature mix is 95.1% buildings to 4.9% roads** (Chad: 2,655,881 building polygons, 136,166 road features). Stratum 3 may be described as settlement mapping density. | Had roads dominated, the variable would have been a road-mapping index and describing it as settlement density would have been wrong. Re-checked per country as the run proceeds. |
| 2026-08-05 | **Bug found and fixed in the population harness: it wrote the parquet once at the end, so a kill during country 14 destroyed thirteen countries of completed work** (30,495 basins summed, 251 saved). Now checkpoints after every country, resumes by skipping already-populated countries, and deletes partial downloads instead of leaving fragments that look complete. | An hour of bandwidth lost to a design fault that was avoidable. Logged rather than quietly fixed, because the same fault in the analysis stage would destroy a single-shot unblinded run. |
| 2026-08-05 | **DR Congo failed to download and was silently marked "no raster" — 17,900 basins, 7.8% of the study region.** Its file exists (1.04 GB); the connection dropped and left a 97 MB fragment. The harness now retries three times and, at the end, reports the basin count behind every failed country as a share of the study region. | A missing country is not a cosmetic gap: it removes those basins from every population-weighted figure, and the countries most likely to fail on a flaky connection are not a random sample. DRC is precisely the large, poorly gauged place this Note is about. |
| 2026-08-04 | **WorldPop UNCONSTRAINED chosen over constrained (maxar_v1), on grounds that go to the study's validity rather than convenience.** The constrained product distributes population only onto detected building footprints. Stratum 3 slices basins by OSM feature density. A settlement-derived population weight compared against a settlement-mapping stratum would **manufacture the correlation the study is testing**. Unconstrained is modelled from census and covariates without that footprint dependency, keeping weight and stratum independent. | Costs ~50x the bytes (Djibouti 10.5 MB vs 202 kB); streaming makes that bandwidth, not disk. This is the circularity attack the red team named as the sharpest technical objection, and choosing constrained would have walked straight into it. Recorded in the harness source as well as here. |
| 2026-08-04 | **Rung 3 is reported as a range with definitions, not a single number.** HydroSHEDS "af" region: **230,028**. Geometrically inside African countries (geoBoundaries ADM0, representative points): **229,744**. Offshore/unmatched: 146. Outside the study region entirely (Spain, Yemen, Portugal, France, Italy): 138. **The developer's own `hybas_country_list.csv` is blank or "None" for 34,496 of them.** | The HydroSHEDS "af" region is not the set of basins in African countries, and country-name matching against the developer's list gave yet another figure (224,827). Three plausible numbers for one rung is exactly why the ladder exists. Geometric assignment governs, and the disagreement count is reported per PROTOCOL §4. |
| 2026-08-04 | **Gate C PASSES at 100.0%** — all 6,772 African inventory points assign to a level-12 basin in `hybas_af_lev12_v1c`. No 10 km buffer fallback needed. | Independent check on the developer's own `HYBAS_ID` assignments against the basin file we hold, not a re-derivation of them. |
| 2026-08-04 | Harness streams rasters — download, zonal sum, manifest, delete — rather than holding the layers. **Machine has 5.7 GB free against ~16 GB of required layers.** Every deleted raster is hashed into `MANIFEST.csv` first, so the run reproduces without the bytes being retained. | A real constraint, surfaced rather than worked around by quietly downgrading resolution. Peak disk stays under a gigabyte. |
| 2026-08-04 | `--surface ghspop` **exits with an error rather than running**, because GHS-POP ships as Mollweide tiles and would otherwise fall through to the WorldPop URL. | Silently summing WorldPop into `pop_ghspop` would make Gate E compare a surface against itself and report a spread of zero — a fabricated null on the study's largest uncertainty. Failing loudly is the only safe behaviour. |
| 2026-08-04 | Zonal statistic is a **sum with nodata excluded**, never nodata-as-zero. | A basin of nodata is unknown, not empty. That is the same distinction — unmeasured versus zero — that the entire Note is about, and getting it wrong in the harness would be self-refuting. |
| 2026-08-04 | **Protocol v1.5.** sha256 `b84f1d7b79f75b378f3ef68ab086ce3f2da64aaa214c443a0e3e2a2e1a4b51ce`, tag `QL-2026-01-protocol-v1.5`. Chain: v1.0 `52ed6235…` → v1.1 `c7f1d8cb…` → v1.2 `965776cd…` → v1.3 `1c395218…` → v1.4 `6764e8a6…` → v1.5 `b84f1d7b…`. | Gate H closed; denominator ladder adopted. |
| 2026-08-04 | **GATE H CLOSED — to the unresolved branch, on evidence, not for want of trying. Both readings are ruled out.** The 3,682 `hybas_*` rows carry **zero** station-derived fields; the ID is self-referential (`hybas_`+`HYBAS_ID`, `HYBAS_ID == original_hybas_id`, all 3,682); **only 71 of 3,682 share a basin with a real African gauge (98% disjoint)**; they concentrate where gauges are absent (South Sudan 1,145, Angola 552, Chad 422, Nigeria 149) against a real-gauge distribution 55% South Africa; and they are **1.6% of the 224,827 African level-12 basins**. `provider 23` / `catalogue.csv` is undocumented in every primary source checked. | The disjointness and complementary geography kill "bookkeeping duplicates". The 1.6% share kills "this is the ungauged forecast surface" as a complete account. What they are is genuinely undocumented, so the gate's condition is not met and its unresolved branch applies: **two denominators, no single headline figure.** This is the honest disposition, not indecision. |
| 2026-08-04 | **The denominator becomes a four-rung ladder, all rungs reported, primary metric computed at each.** (1) 741 African gauges with published metrics; (2) 3,090 real African gauges in the inventory (6,772 rows incl. the 3,682 undocumented); (3) **224,827 African HydroBASINS level-12 basins** (`hybas_country_list.csv`, 1,034,373 rows global, 21.7% African); (4) product display surface ~5,000 verified / ~240,000 lower-confidence — **not enumerable from any release, never used as our denominator.** | A single denominator was always the weakest joint, and Gate H proved no single one is defensible. A ladder is stronger than a choice: it shows how the answer depends on what is counted and removes the temptation to pick the flattering rung. Rung 4 is named and explicitly excluded so no reader wonders whether we forgot it. |
| 2026-08-04 | **Quarantined, and recorded precisely so a future session cannot adopt it: the claim that the model "provides predictions over 1.03 million HydroBASINS level-12 watersheds" is NOT established.** It came from a search summary, and its apparent corroboration — `hybas_country_list.csv` having 1,034,373 rows — is a coincidence of dataset size, not evidence: ~1.03M is how large global HydroBASINS level-12 is. Flood Hub documentation confirms only "approximately 16,000 streamflow gauges" for **training**. | Identical failure mode to the Africa F1 error: a plausible secondary number apparently corroborated. The coincidence is seductive enough that memory would probably accept it later, so it goes in the protocol as a prohibition rather than a note. |
| 2026-08-04 | **Gate G upgraded: the right of reply carries four specific questions**, sent with the draft — what `provider 23`/`catalogue.csv` designate and whether forecasts are issued at those 3,682 locations; how many African forecast points exist, verified and lower-confidence; which version serves Africa and whether per-gauge African metrics are published for it; whether any evaluation of the current production model in Africa exists or is planned. **Unanswered questions are published with the Note, including "no answer received".** | Each is settleable by the subject in a sentence and unsettleable by us at any price. Publishing what went unanswered is not a rebuke — it is the record of what public evidence cannot support, which is this Note's actual subject. |
| 2026-08-04 | Residual risk restated after Gate H: the danger is no longer a wrong headline number — there is none — but a reader treating the top rung (741 of 224,827) as the finding when it measures the distance between an evaluation set and a basin inventory. Every rung gets a sentence saying what it is and is not. | Logged because the ladder removes one failure mode and creates a subtler one. |
| 2026-08-04 | **Protocol v1.4.** sha256 `6764e8a6935b5869cdeaced4c5862866f0498660d1ca73e110ac0de986b3217c`, tag `QL-2026-01-protocol-v1.4`. Chain: v1.0 `52ed6235…` → v1.1 `c7f1d8cb…` → v1.2 `965776cd…` → v1.3 `1c395218…` → v1.4 `6764e8a6…`. | `google-research/flood-forecasting` found and analysed; version question resolved. |
| 2026-08-04 | **THE VERSION QUESTION IS RESOLVED, from the developer's own repository.** `google-research/flood-forecasting` (OpenHydroNet), commit `affbaaa` (2026-06-16), manifested: Handoff-Forecast-LSTM is the **"Former production model"** (Nearing et al., Nature 2024); Mean-Embedding-Forecast-LSTM is the **"Current production model (as of December 2025)"** (Gauch et al., HESS 29:6221, 2025). **The architecture whose per-gauge evidence this study maps is the developer's own designated former production model.** | PROTOCOL v1.0 §1 said we could not establish this and would say so rather than assume. We can now, with their date attached — the as-of is December 2025, repo HEAD is 2026-06-16, and we claim nothing about August 2026 beyond what they state. This was the Note's largest unfixable weakness (red-team item 2) and it is now a citable fact that strengthens the premise. |
| 2026-08-04 | **Access tier is now a choice, and is recorded as one. Pretrained Flood Hub weights are public** (79 MB, `model_epoch055.pt` + config + scalers). White-box access is available. **Declined; the study stays no-query.** | This Note is about what has been published *about* forecast points, not about what a model can be made to do. Running the weights would generate new evidence and answer a different question. Stated explicitly so a reader who knows the weights are public does not read our tier as ignorance. |
| 2026-08-04 | **A second published per-gauge metrics artefact brought into scope: `pretrained-models/.../test_metrics.csv`, 10,137 basins with NSE and KGE, ZERO African** (7 Caravan datasets: hysets 6,392, camelsbr 870, lamah 825, camelsgb 671, camels 670, camelscl 491, camelsaus 218). Unusable as skill evidence regardless — the developer warns the models saw the full 1982–2023 period with **no temporal holdout**, making any evaluation "fundamentally invalid, artificially inflated … in-sample". | A referee would ask why we ignored published per-basin metrics. We did not. They add nothing to Africa's evidence base for two independent reasons, one of which is the developer's own disclosure — and reporting both is more informative than reporting neither. Structure and coverage only were read; no skill value. |
| 2026-08-04 | **Recorded with deliberately tight scope: the operational-settings training basin list has no African basins.** `multimet-basins-list-without-chirps.txt` — the `train_basin_file` for the config the developer says replicates current operational FloodHub training settings "as closely as possible within this open-source framework" — is **15,955 basins, 7 Caravan datasets, 0 African**; North America 80.4%. **Permitted claim: the operational model's African behaviour cannot be independently replicated from released artefacts. Forbidden claim: the model was not trained on African data.** | The binding constraint is redistribution rights — the 2024 work itself used 741 African GRDC gauges that cannot be redistributed. The true and the false sentence here are one word apart, and the false one would end the Note. Wording pre-committed before drafting. |
| 2026-08-04 | Credit for the developer's transparency is made a requirement of the Note's text, near the front, in our own voice: production architectures open-sourced, weights released, and a blunt published warning against the most likely misuse of those weights. | A Note about gaps in published evidence that omits what *has* been published is not a fair account and would deserve its reception. |
| 2026-08-04 | **Gate H is now the only unresolved item that can sink this Note.** Priority raised accordingly. | With the version question resolved, Gate H is no longer one of two hazards to weigh against each other. It is the hazard. |
| 2026-08-04 | **Protocol v1.3.** sha256 `1c395218a2c4687e7acd8af1e3069705cbab107d26eb13fce41e8803375c9cb9`, tag `QL-2026-01-protocol-v1.3`. Chain: v1.0 `52ed6235…` → v1.1 `c7f1d8cb…` → v1.2 `965776cd…` → v1.3 `1c395218…`. | Week-one pre-mortem run and its disposition table converted into protocol commitments. |
| 2026-08-04 | **Pre-mortem run in week one, per the standing rule, against protocol v1.2 and before the strata are built.** `06-report/RED-TEAM.md`. | The rule exists because a pre-mortem is only useful while the design can still change. It paid for itself immediately — see the next row. |
| 2026-08-04 | **NEW GATE H, and a partial retraction of my own correction from earlier today. The inference that the 3,682 `hybas_*` entries are not forecast points is NOT established, and excluding them may be an over-correction in the subject's favour.** They are all African, appear on no other continent, carry coordinates and HydroBASINS IDs, and 676 carry river names — which is what a table of *ungauged prediction points* would look like for a model whose entire contribution is prediction in ungauged basins. Gate H: establish what `provider 23` / `catalogue.csv` designates from primary sources before the primary metric is computed. Unresolved ⇒ two denominators, no headline figure. | The facts in the earlier row are all verified. The inference drawn from them was not, and it sat in this log among six things that were, which made it look established. Reversing it moves the African denominator 3,090 ↔ 6,772 — potentially more than every other design choice combined. Caught by the pre-mortem, in week one, at a cost of one hour. |
| 2026-08-04 | Language commitment: **"measured" is banned** from the title, abstract and headline claims; the word is **"published"**. | "X million Africans live behind forecasts whose accuracy has never been measured" is the sentence a journalist will lift, and every word is defensible except the one nobody notices. Google may have measured skill everywhere internally. Absence of a *published* metric is not absence of measurement, and a black-box tier cannot distinguish them. |
| 2026-08-04 | The historical gauge network is named as the most defensible alternative explanation, and GRDC record length and station density are reported in the same figure as the population weighting. | If evidence coverage tracks where gauges have existed for a century — colonial-era siting, war, structural adjustment — then the pattern is not a decision by the developer, and a Note that lets a reader take it as an indictment is at fault, not the reader. Mechanism is rung C and we are black box: report the association, never the cause. |
| 2026-08-04 | The Note will state **in its opening** that if the developer publishes coverage figures superseding ours, that is the outcome we wanted. | The likeliest good-faith rebuttal — "that is a 2024 supplementary table, our system serves something else, here is the real coverage" — would be right, better than ours, and good for the world. Conceding it in advance costs nothing and removes the sting. |
| 2026-08-04 | **Protocol v1.2.** sha256 `965776cde6c90a5aad2a7d3cf121978663677e4a02831415f1441ae96174584e`, tag `QL-2026-01-protocol-v1.2`. Chain: v1.0 `52ed6235…` → v1.1 `c7f1d8cb…` → v1.2 `965776cd…`, all tagged, frozen text never edited. | Amendments for the Africa F1 correction and the experiment/period specification. |
| 2026-08-04 | **RESOLVED, against us: the "Africa F1 = 0.15–0.21 at the 5-year return period" figure in our background notes is WRONG. Those are the South America row read across two models.** Verified from committed cell outputs in `figure_4_continent_reliability_scores_distributions.ipynb` at pinned commit `1e88caf` — cell 21 (GloFAS) and cell 23 (AI model). **Africa at 5-yr, 0-day lead: GloFAS 0.169748, AI 0.262328.** South America: 0.147253 / 0.210967. Africa is *second* lowest, not lowest; the AI model's African mean is ~55% above GloFAS's. | Caught before it was used in any claim. The paper's prose reconciles exactly to South America to two decimals (0.1473→0.15, 0.2110→0.21, 0.3199→0.32, 0.4564→0.46), so the misattribution originated in our own notes, not the paper. The arithmetic alone was sufficient to condemn it: if South America is the lowest-scoring continent at 0.21, an African mean of 0.15 is impossible. |
| 2026-08-04 | **The "not a rebuttal" framing is narrowed. The Note may say Africa's performance is *recoverable from the published artefacts*, citing Fig. 4 plus the repository. It may not say the paper *stated* Africa's score.** | The paper's text discloses no Africa F1. Fig. 4 is boxplots — medians and quartiles — so the continent means are in neither the article nor the Extended Data; they exist only in committed cell outputs and are recomputable from Zenodo. The stronger reading would not survive a check by anyone who opens the paper, and this study's whole posture depends on being exact about what the developer did and did not publish. |
| 2026-08-04 | **Recorded: the Africa result that IS in the paper's text is the null at the 1-year return period** — the one continent where the AI model does not beat GloFAS. Mean difference 0.004197, p = 0.069605, d = 0.028121 (cell 27), matching the published P = 0.07, d = 0.03. At the 5-year return period the picture is quite different. | Which return period is cited changes the story substantially, so the Note states which one it means every time it makes a comparison. Logged so that choice is visibly deliberate rather than convenient. |
| 2026-08-04 | **Experiment for Q4 specified, having been under-specified at freeze. `EXPERIMENT_NAMES['kfold_splits'] = 'AI Model'`, `EXPERIMENT_NAMES['full_run'] = 'Gauged Basins Run'`** (`evaluation_utils.py:32–34`) — what the paper calls the AI model is `kfold_splits`, **not** the `full_run` our protocol had named. The paper's Fig. 4 also reads the **1980** datasets, not 2014. *Evidenced* stays on `full_run` (presence test, covers all 5,678, most generous). Q4 skill now reports `kfold_splits` (paper anchor), `continent_splits` (Africa held out — the ungauged-relevant cut) and `full_run` (gauged, optimistic) side by side; 2014 primary as frozen, 1980 alongside. | Found before any value was loaded. A population-weighted skill distribution built on `full_run` alone would have described the model where it was **trained**, while this Note's subject is forecast points where it was not. No reader could have caught that from our figures. No experiment is designated the headline until all three exist. |
| 2026-08-04 | Correction appended to `SCOPING.md` rather than edited into it, with the wrong sentence left standing above it. | A scoping document that silently acquires the right answer is worth nothing as a record of what was believed when the study was accepted. |
| 2026-08-04 | **Protocol v1.1.** Amendments appended to §11 for Gates A and B and their consequences. sha256 `c7f1d8cb2a40e41e1ca0cc13eea4988c35ebf5b6272bb10a5a54d12cae5b92e7`, tag `QL-2026-01-protocol-v1.1`. **v1.0 text remains immutably verifiable**: `git show QL-2026-01-protocol-v1.0:…/PROTOCOL.md \| shasum -a 256` returns `52ed6235…`, confirmed. | Frozen text was not edited; §11 was appended to, which is what §11 is for. Both states are tagged, so anyone can diff v1.0 against v1.1 and see exactly what changed and when. Every amendment was made before any metric value was read. |
| 2026-08-04 | **Gate B RESOLVED — Q4 survives.** The three rows per return period are tolerance windows of **0, 1 and 2 days**, from `return_period_metrics.py` (`RETURN_PERIOD_TIME_WINDOWS`, `MultiIndex.from_product`). Columns are lead times 0–7. Metric of record now specifies **window = 2 days**. | Read from the released code at pinned commit `1e88caf`, not inferred. The 2-day window matches the paper's own definition — "both crossed their respective return period threshold flow values within two days of each other" — so our figures are comparable to theirs. Windows 0 and 1 become a sensitivity rather than a choice. |
| 2026-08-04 | **Gate A RESOLVED against the deployment-surface reading — conservative frame applies.** `hybas_gauges_info_lev12.csv` is the research inventory that maps gauges to HydroBASINS level-12 polygons for building cross-validation splits, not the Flood Hub serving inventory. | Three independent lines: its only use in the released code is as a basin lookup (`create_ungauged_experiments_gauge_groups.ipynb` cell 10 — "we will only keep one gauge per HyBAS level 12 polygon"); operational fields are empty, with `is_alerting` set on **2 of 41,905** rows; and no product figure (~5,000 verified, ~240,000 lower-confidence) appears anywhere in the release. The paper says only that the system operates in "over 80 countries" and gives no gauge count. Absent a citable source, the frame is not upgraded. |
| 2026-08-04 | **Supersedes the 2026-08-03 row characterising the inventory as "41,905 gauges / 6,772 African".** The African figure is **not** a gauge count: 3,682 of the 6,772 are `hybas_*` entries — HydroBASINS polygon identifiers with no station code, no site name and no provider station, all of them African. Real African gauges number **3,090** (GRDC 1,683, DWS South Africa 1,295, MAWLR Namibia 112). | Caught before the number was used anywhere. Reporting 6,772 as African forecast points would have inflated the denominator by 119% and made the headline wrong in the subject's disfavour — the exact failure this study is most at risk of. The two categories are now counted and reported separately. |
| 2026-08-04 | **The 5,678-vs-4,089 gap is documented and is probably not empty files.** The paper excludes gauges from evaluation "because it was not possible to match those gauges to a GloFAS pixel", plus drainage area < 500 km² and > 10% drainage-area disagreement — all **exclusions for the GloFAS comparison**. A gauge can carry published Google metrics without being in the 4,089 comparison set. Supersedes the "open" status of the 2026-08-03 row. | Documentary, from the paper. Supports rather than undermines the frozen generous definition of *evidenced*, and lowers expected exposure to Gate D. The null count is still computed at analysis time under the frozen definition — nothing here is assumed in place of measuring it. |
| 2026-08-04 | **Discovered in the released code: precision and recall can be degenerate 1.0.** `_true_positives_fraction_in_window()` returns 1 when there are no observed *and* no predicted events, and 1 when no observed data surrounds any predicted event; 0 when there are no predicted events but observed events exist. A gauge can score perfectly because nothing ever happened there. | Does not change the frozen definition of *evidenced*. It does change Q4: degenerate values are counted and reported per return period, and Q4 is reported both including and excluding them, with neither designated the headline until both exist. Rarest events are where this bites hardest, which is where the study is most likely to be quoted. |
| 2026-08-04 | **Flagged for direct verification before it is cited: the "Africa F1 = 0.15–0.21 at the 5-year return period" figure carried in our background notes.** A secondary reading of the paper reports South America as the *lowest*-scoring continent at mean F1 0.21 and Southwest Pacific highest at 0.46, which cannot be reconciled with an African continent mean of 0.15. | Not resolved from a summary, and not repeated until read directly from the paper's figures and Extended Data. This is load-bearing: the claim that the Note is not a rebuttal rests on Africa's F1 already being disclosed. If the figure is wrong, the framing paragraph changes. Either the 0.15–0.21 range is narrower than a continent mean or our note is mistaken; both are cheap to check and neither may be guessed. |
| 2026-08-04 | Acquired and manifested the paper's analysis code, pinned at commit `1e88caf` (Apache 2.0), rather than citing GitHub loosely. | Gates A and B are answered from this code, so it is evidence and gets a hash like any other input. CEC1 on the v2 preprint objected that GitHub is not a permanent archive; that objection applies to us too. |
| 2026-08-04 | **Protocol v1.0 frozen.** sha256 `52ed62354fe58577c77661de5d20dc92a2991db84ad126d92c375e245efb71da`, frozen 2026-08-04T15:03:31Z, tag `QL-2026-01-protocol-v1.0`. Drafted 2026-08-03, read and approved 2026-08-04. | Frozen before any metric value was read — the day-1 check established identifiers, coordinates and countries only. Header records drafted and frozen dates separately so neither disagrees with the tag timestamp. From here, amendments are appended to §11 with a date and reason; the frozen text is never edited. Anyone can re-hash the published file and compare. |
| 2026-08-03 | Recorded as an observation, not a finding: the 741 evaluated African gauges span 31 countries, of which South Africa holds 273 (37%) and Nigeria holds 8. | Presence and location, not skill — admissible pre-protocol. Logged now because it is the kind of number that later looks like it was discovered while hunting for a headline. It was not; it fell out of the join check. |
